/**
 * Legacy ceremony entry — unused after Phase 3 (help HTML only).
 * Kept so old cached index.html with script tags does not 404.
 */
document.getElementById("status")?.remove();
console.info(
  "[approve] Ceremony moved to Operator Console. Open http://localhost:9470/"
);
