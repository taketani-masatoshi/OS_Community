# Community 開発入口

OOO 横断規約: 親ディレクトリの `AGENTS.md`（またはクローン元の OOO ルート）。  
このリポジトリのアプリ規約は `.cursor/rules/` と各パッケージ README。

- 概要サイトの**編集正本**は OOO `Web/`。ここ `sites/coming-soon` は公開ミラー。
- `npm run overview:links` の既定出力は `sites/coming-soon`。Web から呼ぶときは `OVERVIEW_DIR` / `--dir` で上書きする。
